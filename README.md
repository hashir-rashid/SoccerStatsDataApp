# SoccerStatsDataApp

## Setup
Clone the repo or download all the code. Make sure you have PHP installed and setup. Run the `index.php` file, which should boot the local server and display the site using localhost.
